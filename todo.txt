


Token Expiry 
    Expire code / access tokens
    
    
    
client - redirectUri - could be a collection
 

logging 

implement caching of scope uri lookup 

Remember original request on redirect to login page

client: Edit client, edit redirect URL
 
 
 menu access rules should integrate with access filters 
   (e.g. "/admin" should only be seen my admins)
   
Edit user roles - for admins


Things that would be nice to have in framework
- Authorization - for menus and URLs
- Nice/easy form/menus generation (help text, pop up handlers, etc.)
- Integrated use of CSS framework (Bootstrap)
- html Escaping of input text  (done with h fn in hiccup core) 
- i18n 


 